= PROMPT_REPRISE — codex-gradle
:toc:

_Date_ : 2026-05-11
_Session terminee_ : 017 ✅ (CDX-11 codexEngine TERMINÉ)
_Build_ : 75/75 PASS, compile clean 0 warning
_CI/CD_ : `.github/workflows/test.yml`

'''
== Mission de la Session 018

*[STRATEGIE]* La prochaine session pourra etre consacree a :
- Tests d'integration end-to-end codexIngest + codexRetrieve avec
  pgvector reel (necessite PostgreSQL + pgvector local pour CI)
- Optimisation : batching ONNX pour ingestion multi-documents
- Ajout tache composite `codexPipeline` auto-detection format (PDF/EPUB)
- Exposition REST via engine (l'API Edster SaaS qui consomme assembleCompositeContext)

'''
== Etat du Pipeline (Session 017 — CDX-11 termine)

10 taches enregistrees dans le groupe `codex` +
1 contrat d'interface publique `CodexVectorStore` consomme par engine N3 :

. extractText
. extractBookStructure
. extractEpubStructure
. asciiDocToJsonLdd
. importBookSql
. convertToMarkdown
. chunkDocument
. exportKnowledgeBase
. codexIngest (ONNX → pgvector)
. codexRetrieve (cosine similarity → corpus requetable)

+ `CodexVectorStore` dans `dev.cheroliv.codex.store` — API publique pour engine N3 :
  `search(query, topK)` suspend / `searchBlocking(query, topK)` bloquant

12 classes de test, 75/75 PASS, 0 failure, 0 skipped.

CI/CD : `.github/workflows/test.yml` — `publishToMavenLocal` + `:doc-pipeline:check` sur JDK 24.

'''
== Integration Engine N3 (CDX-11)

Engine (N3) dispose maintenant de :
- `./engine assemble [query]` — `assembleCompositeContext`
  - `codexRetrieve` via ProcessBuilder (fallback propre sans pgvector)
  - Knowledge Graph (`office/graph.json`)
  - `augmentOpencode` codebase-gradle
  - Sortie `build/engine/composite-context.json`
  - Mode : `-Pcontext=composite|rag|kg|sql|none`

'''
== Conventions pgvector

[source,kotlin]
----
codex {
    zone = LicenseZone.OSS
    pgvectorHost = "localhost"
    pgvectorPort = "5432"
    pgvectorDatabase = "codex"
    pgvectorUser = "codex"
    pgvectorPassword = "codex"
}
----

'''
== Commandes rapides

[source,bash]
----
# Compilation
./gradlew :doc-pipeline:compileKotlin

# Tests
./gradlew :doc-pipeline:test

# Check complet (compile + tests + Kover)
./gradlew :doc-pipeline:check

# Liste des taches
./gradlew :doc-pipeline:tasks --group codex

# Engine end-to-end
cd ../engine && ./engine.sh assemble "ma question"
----
