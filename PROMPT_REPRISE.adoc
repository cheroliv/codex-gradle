= PROMPT_REPRISE — codex-gradle
:toc:

_Date_ : 2026-05-12
_Session terminee_ : 020 ✅ — Code Review Globale (audit passif)
_Build_ : 79/79 PASS (75 unit + 4 integration), compile clean 0 warning
_CI/CD_ : `.github/workflows/test.yml`

'''
== Mission de la Session 021

*[STRATEGIE]* Exposition REST via engine N3 — API Edster SaaS qui consomme assembleCompositeContext.

'''
== Etat du Pipeline (Session 018 — CDX-12 termine)

11 taches enregistrees dans le groupe `codex` +
1 contrat d'interface publique `CodexVectorStore` consomme par engine N3 :

. extractText
. extractBookStructure
. extractEpubStructure
. asciiDocToJsonLdd
. importBookSql
. convertToMarkdown
. chunkDocument
. exportKnowledgeBase
. codexIngest (ONNX → pgvector, batching paramétrable `batchSize=32`)
. codexRetrieve (cosine similarity → corpus requetable)
. codexPipeline (auto-détection PDF/EPUB → extraction → Markdown → chunking → JSON PipelineResult)

+ `CodexVectorStore` dans `dev.cheroliv.codex.store` — API publique pour engine N3 :
  `search(query, topK)` suspend / `searchBlocking(query, topK)` bloquant

13 classes de test (12 unit + 1 integration), 79/79 PASS, 0 failure, 0 skipped.

CI/CD : `.github/workflows/test.yml` — Docker natif sur `ubuntu-latest`, TestContainers pgvector intégré.

'''
== Nouveautes CDX-12

=== ONNX Batching
- `CodexIngestTask.batchSize` paramétrable (convention: 32)
- Les chunks sont groupés par lots avant vectorisation → réduction des appels ONNX

=== Tache composite codexPipeline
- Auto-détection PDF/EPUB via extension de fichier
- Compose extractBookStructure/ExtractEpubStructure + convertToMarkdown + chunkDocument
- Sortie JSON `PipelineResult` (format, license, docCount, chunkCount)

=== Tests integration pgvector
- `CodexIngestRetrieveIT.kt` — 4 tests avec PostgreSQLContainer pgvector/pgvector:pg16
- Test end-to-end : ingest 3 chunks → retrieve cosine similarity (seuil > 0.6)
- Test batching batchSize=1 → 5 chunks tous retrievable
- Test codexPipeline auto-détection format PDF
- docker-java 3.7.0 + testcontainers 1.21.4 (aligné slider-gradle/plantuml-gradle)

'''
== Conventions pgvector

[source,kotlin]
----
codex {
    zone = LicenseZone.OSS
    pgvectorHost = "localhost"
    pgvectorPort = "5432"
    pgvectorDatabase = "codex"
    pgvectorUser = "codex"
    pgvectorPassword = "codex"
}
----

'''
== Commandes rapides

[source,bash]
----
# Compilation
./gradlew :doc-pipeline:compileKotlin

# Tests unitaires
./gradlew :doc-pipeline:test --tests "*Test"

# Tests integration (necessite Docker)
./gradlew :doc-pipeline:test --tests "*IT"

# Tous les tests
./gradlew :doc-pipeline:test

# Check complet (compile + tests + Kover)
./gradlew :doc-pipeline:check

# Pipeline composite
./gradlew codexPipeline --sourceFile=/path/to/doc.pdf

# Engine end-to-end
cd ../engine && ./engine.sh assemble "ma question"
----
